// Small initialization script extracted from index.html
// Ensures the page gets the 'js' class when JavaScript is enabled
document.documentElement.classList.remove('no-js');
document.documentElement.classList.add('js');

export default {};
